package data;

public class GroundStationData {
	
	/*
	 * lat, long
	 * 
	 * what other ground stations is this connected to?
	 * What packet sending interval distrobution?
	 * 
	 * when does it start? defaults to 0
	 * when does it end? used to pick a simulation length?
	 * 
	 * does it move?
	 */
}
